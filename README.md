# techsoluxapi

## How to run this application?

clone this project using the following command

1. `git clone https://github.com/HakimAsa/techsoluxapi.git`
2. run `yarn` or `npm install`.
3. Set environment variables securely via git bash or cmd
